/* Hero sekcija */
.hero-section {
    position: relative;
    background-image: url('path_to_your_background_image.jpg'); /* Zamijenite s putanjom do vaše slike */
    background-size: cover;
    background-position: center;
    height: 100vh; /* Puni visinu preglednika */
    color: #ffffff; /* Bijela boja teksta */
    display: flex;
    align-items: center;
}

.hero-section .overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5); /* Crna boja s 50% prozirnošću */
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1; /* Osigurava da se overlay prikazuje iznad slike */
}

.hero-section .container {
    position: relative;
    z-index: 2; /* Osigurava da se tekst prikazuje iznad overlay-a */
}

.hero-section h1, .hero-section p {
    margin: 0;
}
